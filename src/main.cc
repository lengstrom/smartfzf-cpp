#include <iostream>
#include <vector>
#include "smartfzf.h"
#include "renderer.h"

int main(int argc, char *argv[]) {
    std::cout << "smartfzf works!" << std::endl;
    std::vector lines_to_render;
    
        // Get window dimensions
        // Initialize window
    
    return 0;
}
