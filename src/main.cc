#include <iostream>
#include <vector>
#include "smartfzf.h"
#include "renderer.h"

int main(int argc, char *argv[]) {
    std::cout << "smartfzf works!" << std::endl;
    std::vector lines;
    Renderer r()
        // Get window dimensions
        // Initialize window
    
    return 0;
}
