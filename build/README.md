#smartfzf build directory

This directory exists so you can build
the project! On unix, do the following,
once in this directory, in order to build
and run the project:

    cmake ..
    make
    ./smartfzf
    
